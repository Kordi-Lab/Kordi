# Agent transcript regression fixture

`long-reply-follow-up.png` is a synthetic iOS Simulator capture from
`ConversationAgentSendIntegrationTests.testSendingAfterLongAgentReplyKeepsTheRequestVisible`.
It shows a staged follow-up below a generated long reply. The test uses an in-memory
message store and preview data; the capture contains no user conversation or account data.
