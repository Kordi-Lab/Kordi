# Rolling digest screenshots

These screenshots render the actual desktop digest components using the synthetic model-evaluation snapshot described in `docs/design-previews/rolling-digest/evaluation.md`. All conversation content and participants are fictional. Cloud API responses were intercepted during capture; no personal calendar data or account credentials appear in the images.

Use this directory only for reviewed product illustrations built from synthetic data.
