# Message forwarding review captures

These screenshots show the implemented macOS component and iOS Beta simulator
using synthetic, offline preview conversations. No real messages, accounts,
credentials, or infrastructure details are included.

The standalone HTML proposal in `docs/design-previews/forward-message` predates
the final compact confirmation; the screenshots here show the final UI.
