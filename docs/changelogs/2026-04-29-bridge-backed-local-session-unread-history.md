# Bridge-backed local session history and unread clearing

- Preserved canonical shared-session history when a local runtime transcript has richer owned-agent tool details.
- Treat UUID local chat sessions with Bridge participants as visible local sessions for unread clearing, while keeping raw Bridge conversations scoped to Bridge read handling.
