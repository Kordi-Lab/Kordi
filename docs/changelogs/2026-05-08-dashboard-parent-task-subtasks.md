# Dashboard parent task subtasks

- Nest durable `task_operator` creates with `parentTaskId` under the matching existing task row instead of showing them as separate top-level tasks.
- Parse durable task IDs from `task_operator` result text in the dashboard so opaque IDs generated by the runtime can be used for parent/subtask matching.
