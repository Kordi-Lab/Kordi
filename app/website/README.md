# Kordi AI Website

A standalone Vite-powered PR/landing page for Kordi AI.

## Commands

Run from the monorepo root:

```bash
pnpm dev:website
pnpm test:website
pnpm build:website
```

The site lives separately from the desktop product UI and uses a restrained watercolor paper visual identity.
